# Development and test dependencies. Not installed in the production image.
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
mongomock>=4.3.0        # in-memory MongoDB so tests need no server
httpx>=0.27.0           # starlette.testclient requires this for TestClient
ruff>=0.6.0             # lint and format
